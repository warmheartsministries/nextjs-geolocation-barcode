export type Customer = {
  id: string;
  name: string;
  email: string;
  password: string;
  image_url: string;
};

export type Product = {
  id: string;
  upc: string;
  brand: string;
  description: string;
  img_url: string;
  price: string;
};
